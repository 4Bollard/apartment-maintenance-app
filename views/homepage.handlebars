


{{!-- {{#each users as |user| }} --}}
<div class="row align-center mb-5">
    <div class="col-md-6">
        <h2>Welcome! {{user.name}}</h2>
    </div>
    <div class="col-md-6">
        {{!-- <h3><a href="mailto:{{user.email}}">{{user.email}}</a></h3> --}}
    </div>
</div>
<div>
    <div class="form-group">
        <button><a class="btn btn-seccondary" type="issue" href="./issue"> maintenance help request</a></button>
    </div>
    <div>
        <h3>Active issues</h3>
        <div id="active-issue"></div>
    </div>
    <div>
        <h3>Completed issues</h3>
        <div id="completed-issue"></div>
    </div>
<script src="/js/homepage.js"></script>
</div>
{{!-- {{/each}} --}}


{{!-- todo
1-  need to make a funtion that generates html off of the completed issue file

2-  need to have logic in homepage that prevents body content from displaying until logged in.

3-  need to add logic that filters only user issues if user is logged in vs manager.  


 --}}